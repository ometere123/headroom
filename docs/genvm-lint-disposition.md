# GenVM lint findings and disposition

The contract keeps the pinned GenVM Depends header `py-genlayer:1jb45aa8ynh2a9c9xn3b7qqh8sm5q93hwfp7jqmwsfhh8jpz09h6`. The official stable `genvm-linter==0.11.0` reports the same seven E010 reachability warnings, but its `setup` and `validate` cannot resolve the pinned runner archive (`runners/py-genlayer/1j/b45aa8ynh2a9c9xn3b7qqh8sm5q93hwfp7jqmwsfhh8jpz09h6.tar`). The pinned archive is present in the local manager cache. `genvm-linter==0.11.1rc2` resolves the pinned SDK, and `validate` passes; it reports a newer runner only as informational. The contract syntax uses the pinned runner API and remains unchanged.

Both linter versions report exactly seven E010 diagnostics: `_fetch` and the nested custom `leader_fn` closures for admission, change, measurement, incident examination and challenge; `review_change` contributes two sites. These are conservative static reachability warnings around the documented `gl.vm.run_nondet_unsafe` custom leader/validator pattern and its web-fetch helper. They are not suppressed in the raw linter output. `scripts/check_genvm_lint.py` accepts only this exact set of messages and counts and still requires `validate.ok=true`; any new warning or failed validation fails CI. The complete setup/lint/validate/check output is in `genvm-lint-full-output.txt`.

Official guidance documents custom leader and validator functions through `gl.vm.run_nondet_unsafe` and states that validator execution errors must not count as agreement: [Equivalence Principle](https://docs.genlayer.com/developers/intelligent-contracts/equivalence-principle), [non-determinism](https://docs.genlayer.com/developers/intelligent-contracts/features/non-determinism), [error handling](https://docs.genlayer.com/developers/intelligent-contracts/features/error-handling).
