"use client";
import { EXPLORER } from "@/lib/config";
export function TxNotice({phase,hash,error,onRetry}:{phase:string;hash?:string;error?:string;onRetry?:()=>void}){if(!phase&&!error)return null;const interrupted=phase==="monitoring_interrupted";const failed=phase==="failed"||phase==="finalized_error";const label=phase==="signing"?"WALLET SIGNING":phase==="submitted"?"TRANSACTION SUBMITTED":phase==="finalizing"?"PENDING · FINALIZING":phase==="finalized"?"FINALIZED · EXECUTION SUCCEEDED":interrupted?"STATUS CHECK INTERRUPTED":failed?"FAILED":"TRANSACTION";return <div className={`tx ${failed?"tx-error":""}`} role="status"><b>{label}</b>{hash&&<a href={`${EXPLORER}/tx/${hash}`} target="_blank" rel="noreferrer">{hash.slice(0,10)}… ↗</a>}<span>{interrupted?"Your transaction was submitted, but HEADROOM temporarily lost the RPC connection while checking finalization. The transaction may still finalize on-chain.":error}</span>{interrupted&&hash&&onRetry&&<button className="text-link" type="button" onClick={onRetry}>Retry Status</button>}</div>}

