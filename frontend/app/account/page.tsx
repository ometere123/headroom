"use client";import {useEffect,useState} from "react";import {read,write,waitFinal} from "@/lib/contract";import {useInjectedWallet} from "@/lib/wallet";import {TxNotice} from "@/components/TxNotice";
export default function Account(){const w=useInjectedWallet();const[c,setC]=useState("0"),[phase,setPhase]=useState(""),[hash,setHash]=useState(""),[error,setError]=useState("");useEffect(()=>{if(w.address)read("get_credit",[w.address]).then(x=>setC(String(x)))},[w.address]);async function withdraw(){if(!w.address)return;try{const h=await write(w.address,"withdraw_credit",[w.address]);setHash(String(h));setPhase("finalizing");await waitFinal(String(h));setPhase("finalized");setC("0")}catch(e:any){setError(e.message)}}return <section className="shell page"><div className="kicker">settlement rail</div><h1>account.</h1><div className="console" style={{marginTop:38,maxWidth:760}}><div className="console-top"><span>CLAIMABLE CREDIT</span><span>OWNER ONLY</span></div><div className="metric-big">{(Number(c)/1e18).toFixed(4)} GEN</div><p style={{color:"var(--muted)"}}>Incident service credits, returned challenge bonds and closed-provider balances settle here before an owner-directed withdrawal.</p><button className="button primary" onClick={withdraw}>withdraw credit</button><TxNotice phase={phase} hash={hash} error={error}/></div></section>}
