import Link from "next/link";
const blocks=(n:number,on:number)=><>{Array.from({length:n}).map((_,i)=><i className={i<on?"on":""} key={i}/>)}</>;
export default function Home(){return <><section className="shell hero"><div><div className="kicker">sla admission + enforcement / studionet 61999</div><h1>don’t promise<br/>what you can’t serve.</h1><p>HEADROOM makes GenLayer the co-signer before an SLA-backed promise is allowed to exist, then the neutral incident court if prevention fails. Capacity and liability are reserved before admission. Operational changes are preflighted. Failure is adjudicated against the same frozen covenant.</p><div className="hero-actions"><Link className="button primary" href="/covenants">enter control room</Link><Link className="button orange" href="/open">bond a service</Link></div></div><div className="console"><div className="console-top"><span>HEADROOM / PAYMENTS-EU</span><span className="signal">CONSENSUS HEALTHY</span></div><div className="gauge"><div className="gauge-head"><span>operational commitment</span><b>67%</b></div><div className="track">{blocks(20,13)}</div></div><div className="gauge"><div className="gauge-head"><span>liability reserve</span><b>84%</b></div><div className="track liability">{blocks(20,17)}</div></div><div className="request-card"><div><div className="kicker">incoming reservation</div><strong>+ 2,000 RPS / + 6 GEN</strong><div style={{color:"var(--muted)",marginTop:6}}>would leave 11.2% contractual headroom</div></div><div className="decision">admit</div></div></div></section><section className="ticker"><div className="ticker-inner"><span><b>ADMISSION</b> reserve capacity before promise</span><span><b>LIABILITY</b> reserve GEN before exposure</span><span><b>CHANGE CONTROL</b> preflight risky operations</span><span><b>INCIDENT</b> reconstruct facts before blame</span><span><b>SETTLEMENT</b> deterministic credit from bonded liability</span><span><b>ADMISSION</b> reserve capacity before promise</span></div></section></>}
